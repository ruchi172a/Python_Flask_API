FLASK_APP=app
FLASK_DEBUG=True

#If we have the python-dotenv library installed, when we run the flask run command, Flask will read the variables inside .
# flaskenv and use them to configure the Flask app.